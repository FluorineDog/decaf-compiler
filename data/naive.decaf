class Node {
  int x;
  Node next;
}

class NodeExt extends Node {
  int y;
}


class Main {
  int main() {
    Node list;
    Node node;
    Node iter;
    int i;
    NodeExt ext;
    ext.x = 0;
    list = null;
    for(i = 0; i < 100; i = i + 1){
      node = New NodeExt;
      node.x = i;
      node.next = list;
      list = node;
    }
    iter = list;
    while(iter != null){
      Print(iter.x);
      iter = iter.next;
    }
    return 0;
  }
}
