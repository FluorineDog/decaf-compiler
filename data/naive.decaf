class NodeExt extends Node {
  int y;
}
class Node {
  int x;
  Node next;
}



class Main {
  int main() {
    Node list;
    Node node;
    Node iter;
    int i;
    NodeExt ext;
    list = null;
    for(i = 0; i < 100; i = i + 1){
      ext = New NodeExt;
      ext.y = i + 1;
      ext.x = i;
      node = ext;
      node.next = list;
      list = node;
    }
    iter = list;
    while(iter != null){
      Print(iter.x);
      iter = iter.next;
    }
    return 0;
  }
}
