class Node {
  int x;
  Node next;
}

class Main {
  int main() {
    Node list;
    Node node;
    Node iter;
    int i;
    list = null;
    for(i = 0; i < 100; i = i + 1){
      node = New Node;
      node.x = i;
      node.next = list;
      list = node;
    }
    iter = list;
    while(iter != null){
      Print(iter.x);
      iter = iter.next;
    }
    return 0;
  }
}
